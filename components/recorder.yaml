# db_url: /backup
auto_purge: True
purge_keep_days: 2
include:
  domains:
    - sensor
    - light
exclude:
  domains:
    # - automation
    - weblink
    - updater
    - input_boolean
    - input_number
  entities:
    - sensor.date
    - sensor.time
    - sensor.last_boot
    - sun.sun